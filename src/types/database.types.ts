// TODO: Regenerate via `supabase gen types typescript` after a real Supabase
// project is created. The stub lets the typed client (`createClient<Database>`)
// compile today and gives every future contributor a stable import surface.
export interface Database {
  public: {
    Tables: Record<string, never>
    Views: Record<string, never>
    Functions: Record<string, never>
    Enums: Record<string, never>
  }
}
