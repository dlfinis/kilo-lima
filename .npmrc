# pnpm 10's default `trust-policy=no-downgrade` blocks fresh installs
# of Vite 5.4.21 (first 5.4.x published with npm provenance, which
# pnpm 10 reads as a trust downgrade). The package itself is fine;
# this is a policy relaxation. See sdd/foundation/apply-progress.
trust-policy=allow
